// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Book {
  id           Int      @id @default(autoincrement())
  title        String
  author       String
  category     Category
  genre        String?
  form         String?
  format       Format   @default(PHYSICAL)
  languageCode String   @default("en")
  translator   String?
  status       Status   @default(TO_READ)
  rating       Int?
  notes        String?
  createdAt    DateTime @default(now())
  updatedAt    DateTime @updatedAt
}

/*
Book model fields to add:
- ISBN
- Original Language
- Publisher
- Publication Date
- Number of Pages
- Cover image URL - Implement a way to search for the cover image
- Tags - Implement a way to add multiple tags to a book
- Series - Implement a way to link books in a series
- Reading Start Date
- Reading End Date
- Review - Implement a way to add a detailed review for the book
- Short Summary
- Edition?

- Notes - This will be a whole structure on its own
*/

/*
Create enums for form:
For Fiction:
- NOVEL
- NOVELLA
- SHORT_STORY
- SHORT_STORY_COLLECTION
- POETRY_COLLECTION
- EPIC_POEM
- PLAY
- SCREENPLAY
- AUDIO_DRAMA
- COMIC
- GRAPHIC_NOVEL
- MANGA
- MYTHOLOGY
- FANFICTION
- INTERACTIVE_FICTION
- SERIALIZED_STORY

For Non-Fiction:
- BIOGRAPHY
- AUTOBIOGRAPHY
- MEMOIR
- ESSAY
- ESSAY_COLLECTION
- TREATISE
- DISSERTATION or THESIS
- PAPER
- ARTICLE
- JOURNAL
- MANUAL
- TEXTBOOK
- MAGAZINE
- ACADEMIC_PAPER
- NOTEBOOK
- DIARY
*/

enum Category {
  FICTION
  NON_FICTION
}

enum Format {
  PHYSICAL
  EBOOK
  AUDIOBOOK
}

enum Status {
  TO_READ
  READING
  FINISHED
}

/*
Add to Status enums:
- ABANDONED
- PAUSED
*/
